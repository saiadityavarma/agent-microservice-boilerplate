apiVersion: v2
name: agent-service
description: A Helm chart for Agent Service - FastAPI-based agent platform with Celery workers
type: application
version: 1.0.0
appVersion: "1.0.0"

keywords:
  - agent
  - fastapi
  - celery
  - microservice

maintainers:
  - name: Platform Team
    email: platform@example.com

sources:
  - https://github.com/your-org/agent-service

dependencies: []

annotations:
  category: Application
  licenses: Apache-2.0
